+ {{ window_name }}: {{ window_class }} '({{ direction }}) window' '{{ direction }} window'
    firstLocation = {{ inside_symbol }}
    secondLocation = {{ outside_symbol }}
    locationList = [firstLocation, secondLocation]
;